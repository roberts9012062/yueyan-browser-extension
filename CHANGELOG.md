# 变更记录（CHANGELOG）

本插件的全部用户可见变更记录于此。发版时 `public/manifest.json` 的 `version` 必须递增并同步登记。

## [0.27.0] - 2026-08-29

### 新增

- **写说说**（首页底部快捷输入改造）：原「向 AI 提问」输入框升级为说说发布器——文字 + 四类内容（图 / 视频 / 音乐 / 链接）+ 公开·私有一键切换 + 发布，发布为说说（moment 形态，≤2000 字，html 富文本混排）：
  - **图片**：本地多选或直接**粘贴**进输入框，>1MB 自动压缩（长边 2048 / JPEG 85）后经开放网关新端点 `media.upload` 上传站点媒体库，附件条缩略图可删，media_id 随发布关联；
  - **视频**：弹层贴 B站视频页链接 / BV 号 / YouTube 链接，前端解析为官方 iframe 嵌入（player.bilibili.com / youtube.com/embed，域与主站消毒白名单一致），前台直接可播（b23.tv 短链无法前端展开，弹层内明示）；
  - **音乐**：弹层贴网易云歌曲链接（music.163.com/song?id=…），生成与主站同构的音乐引用块（`data-music-embed=netease`），前台渲染自研播放器；
  - **链接**：弹层填 URL + 显示文字，正文插入可点链接（`target=_blank`）；
  - 正文组装与主站富文本导出同构（`<p>` 段落 + 图片 + 视频/音乐嵌入块），纯文本字数实时计数、超 2000 禁发；成功「已发布 ✓」3 秒自散，失败错误条携带后端 message。
  - 插入弹层（视频/音乐/链接）修复：弹层原锚定在底部输入条容器上导致遮罩只盖住一小块、且无显式关闭入口——现改为**全视口覆盖**，右上角新增关闭按钮（遮罩点击与 Esc 同效），打开即自动聚焦输入框可直接粘贴。
- **「生成文章」发布可见性**：AI 助手生成文章面板底部新增「公开 ⇄ 私有」切换胶囊（与写说说共用组件），随发布/存草稿一并提交——此前恒为公开。

### 后端配套（需重启后端生效）

- 开放网关新增 **媒体上传** `POST /api/v1/open/media`（media.upload，目录已登记）：multipart `file` 字段，凭 Key 绑定用户身份复用主站媒体上传链路（类型/大小校验沿用存储层），返回本站地址与 media_id。
- **使用前提**：主站后台为 Key 勾选新接口「媒体上传」（旧 Key 未勾选时上传 403，插件提示重新生成 Key）。

## [0.26.2] - 2026-08-28

### 变更

- **对话输出上限拉满**：`DEFAULT_MAX_TOKENS` 4000 → **16000**（后端 `clampMaxTokens` 的硬顶；传更大的值也会被后端收敛到 16000）。长文/字幕总结不再有任何输出截断顾虑。

## [0.26.1] - 2026-08-28

### 修复

- **生成文章的标题显示 ```json / 标签 SEO 全空**：元信息生成走的是非流式端点，推理模型的思考段（`<think>`）计入输出 token，800 上限内 JSON 未写完即被截断，解析失败后兜底把围栏标记当标题。三层修复：
  - 元信息改走**流式端点聚合**（后端 ThinkFilter 剥思考段），输出上限提到 2000；
  - 解析新增**截断抗性兜底**：整体 JSON 解析失败时逐字段正则提取（title/seo/tags 各自独立救回）；
  - 最终兜底标题跳过 ``` 围栏与 `<think>` 行，不再把标记当标题。

## [0.26.0] - 2026-08-28

### 变更

- **对话全面流式化**（对接开放网关新增的 `ai.chat.stream` SSE 端点）：
  - 回复逐字增量渲染（SSE `{"text"}` 事件逐块追加），自动跟随滚动；Markdown 随流实时重渲染；
  - **联网检索统一到后端**：发送时携带 `web_search`（由输入框联网开关决定），后端先检索并下发 `search_results` 来源事件（气泡下方实时渲染引用列表），检索上下文由后端注入——插件端原"前端检索 + 拼 prompt"链路退役，能力卡「联网搜索」等价于开启开关后发送；
  - 流式失败自动回退非流式一次（兼容旧后端/流中断），已渲染的部分内容保留。

## [0.25.6] - 2026-08-28

### 修复

- **长总结被拦腰截断**：对话输出上限此前写死 800 token——普通聊天够用，长网页/B 站字幕的总结输出到一半即被掐断（非流式下表现为"半截话"）。现提升至 4000（后端 `ai.chat` 上限 16000，文章级输出取平衡值）。

## [0.25.5] - 2026-08-27

### 新增

- **B 站文章嵌入原生播放器**：对接 boke bilibili-video 插件的内容块协议（阅读插件源码确认）——总结视频时采集 `bvid/cid/title/cover/author/duration`，生成文章时在**正文顶部**嵌入 `<div data-plugin-block="bilibili" data-props="{…}"></div>` 播放器块（props 引号按协议转义为 `&quot;`），发布后站点前台直接渲染可播放的视频卡片；尾部「📺 原视频」文字链接保留作备份。

## [0.25.4] - 2026-08-27

### 修复

- **B 站视频文章缺标题/标签/SEO/视频链接**：
  - 视频链接：此前在润色正文里找 BV 号，但视频地址实际在总结消息（`📺 … — 链接`）里，永远匹配不到。现「生成文章」时从消息流提取来源 URL 传入面板，正文尾部稳定附上「📺 原视频」链接（boke 的 B 站插件可渲染播放器）；
  - 元信息生成（标题/标签/SEO）：调整为**先行执行并带一次自动重试**（此前排在耗时的字幕润色之后，长字幕场景偶发超时导致全部留空）；输入收敛到 2000 字降低超时概率；标题为空时重试一次，仍失败才提示手填；
  - 顺序优化：元信息（快）先就位，润色（慢）随后，打开面板更早看到标题。

## [0.25.3] - 2026-08-27

### 修复

- **B 站请求打错域名（返回 HTML 导致 JSON 解析炸）**：注入函数此前用相对路径 `/x/...`，在视频页上被解析为 `www.bilibili.com/x/...`——B 站 API 实际部署在 `api.bilibili.com`，www 域返回 404 HTML 页（即 `Unexpected token '<'` 报错来源）。四个请求（view / nav / player wbi / player v2 回退）全部改为绝对地址；页面上下文跨子域调 API 与 B 站自身行为一致，CORS 与登录态均正常。

## [0.25.2] - 2026-08-27

### 修复

- **wbi 签名的 md5 实现错误**：0.25.1 自写的无符号加法函数有缺陷，md5 输出全 0、`w_rid` 必然无效。已替换为经典实现（Paul Johnston 版），并在 Node 环境对照标准值（`md5("abc")` 等）验证通过后才写入；注入函数的兜底 catch 现在携带真实错误信息（如 `fetch-error(Failed to fetch)`），不再只显示笼统原因。

## [0.25.1] - 2026-08-27

### 修复

- **已登录仍拿不到字幕**：B 站 2023+ 对字幕接口启用 wbi 签名风控，未签名的 `player/v2` 登录态也常返回空字幕列表（此前失败原因统一误报为"未登录"）。现注入函数自包含实现 md5 + wbi 签名（nav 取 img/sub key → 混淆表合成 mixin key → 参数排序加 w_rid），调 `player/wbi/v2` 取字幕，失败自动回退旧接口；失败提示携带真实原因码便于定位。

## [0.25.0] - 2026-08-27

### 新增

- **B 站视频总结**：浏览 `bilibili.com/video/*` 时点「网页总结」自动切换为视频模式：
  - 在视频页上下文（同源请求自动携带登录态）调 B 站接口取 `cid` 与字幕列表，**优先 AI 中文字幕（ai-zh）**、回退 CC 中文字幕；字幕文件在扩展页拉取（主机权限免跨域）拼成全文交 AI 总结（按要点列出 + 开头一句话主题）；
  - 未登录 / 视频无字幕时明确提示；
  - 「生成文章」检测来源为 B 站视频时，正文尾部自动附「📺 原视频」链接（boke 站点的 B 站插件可在正文渲染播放器）。

## [0.24.3] - 2026-08-27

### 修复

- **广告容器误杀全站图片（ybfl.net 实证）**：`[class*="ad" i]` 是子串匹配——广告插件在 `<body>` 上留下的 `aa-prefix-advads-` 标记让**所有图片**向上都能命中"广告容器"，全被过滤（此前 0 张图的真正根因；header/shadow/download 等普通类同样会误中）。改为 token 精确匹配（`ad` / `ad-right` / `advads-*` / `banner` / `promo` 等词法命中）且只查图片自身与 3 层祖先。

## [0.24.2] - 2026-08-27

### 修复

- **教程页图片抓不到（润色文章无原图）**：抓图环节三项加固——
  - 懒加载属性全面兼容：真实地址按 `data-src / data-original / data-actualsrc / data-lazy-src` 优先取（此前仅 data-src，且 src 为占位图时短路拿不到真图）；
  - 协议相对地址（`//cdn.xxx/img.png`）按页面协议归一后再校验，不再被 http 检查误拒；
  - 正文容器（article/main 等）内一图未得时自动全页兜底再扫一次。
- 诊断提示：总结完成但一图未取时明确提示「未在该页检测到可携带的正文图片」，与"抓到但未传递"区分定位。

## [0.24.1] - 2026-08-27

### 修复

- **广告过滤误杀教程步骤图**：0.24.0 的「链接包裹即排除」规则伤及面过大——教程站步骤图几乎都被 `<a>` 包裹做点击放大。改为保守判定：仅当包裹链接指向**外域落地页**（广告跳转特征）才排除；指向图片文件（查看大图）或同域链接的一律保留。按钮包裹、侧栏/页脚/导航位置、广告类名容器三条规则维持不变。

## [0.24.0] - 2026-08-27

### 修复

- **编辑器图片无法选中删除**：contenteditable 对图片的原生点选在 Chromium 中不稳定（点击常被当作拖拽起点）。现点击图片即用程序建立选区（连同包裹的 figure 一起选中，虚线高亮），按 Delete/Backspace 删除——审查时可一键剔除广告图等无关图片；编辑器底部附操作提示，图片 hover 显示虚线框。
- **广告图源头过滤**：网页总结抓图新增启发式排除——被链接/按钮包裹的图（广告位几乎必裹链接）、位于侧栏/页脚/导航的图、容器类名或 ID 含 ad/advert/banner/promo 的图。教程步骤图（正文直排无链接）不受影响。

## [0.23.2] - 2026-08-27

### 变更

- **慢任务超时对齐后端 5 分钟上限**：后端上游超时放宽至 300s（Nginx 代理层已同步 300s）后，插件端原 90s 会先超时。现 `ai.assist`（润色/配图/识图）320s、`ai.chat`（对话/元信息）120s——插件侧始终大于后端上限，保证等到真实结果而非插件自掐。

## [0.23.1] - 2026-08-27

### 修复

- **「生成文章」打开后标题/标签/图片全丢**：初始化原为单一 try——润色一失败（后端 30s 上限，长文偶发超时）整体进兜底，标题标签不填、原文插图一并跳过。现拆为三段独立容错：
  - 润色失败 → 用原文继续；
  - 标题/标签/SEO 生成失败 → 留空提示手填（元信息输出上限 500→800 token，避免带标签后 JSON 被截断）；
  - **原文图片插入与润色成败解耦，无论如何都会插入正文**。
  - 各段失败原因聚合显示在顶部提示条。
- 附：已线上实测 `posts.create`（标题/标签/SEO/`<figure>` 图片 HTML 全字段）——后端落库完整无缺失，该问题纯属插件端初始化容错缺陷。

## [0.23.0] - 2026-08-27

### 新增

- **发布前外链图片自动转存**（对接开放网关新增的 `media.transfer`）：点发布/存草稿时，自动扫描正文里指向其它站点的 `<img>`，逐张转存到站点媒体库——src 替换为本站持久地址、media_id 并入文章关联，从根上解决源站防盗链导致的裂图；
  - 本站图片（AI 配图等）自动跳过不重复转存；转存进度在顶部提示条显示（"正在转存外链图片 n/m…"）；
  - 单张失败（源站拒绝/非图片）保留原地址继续发布并提示失败张数，不阻断整个发布流程。

## [0.22.0] - 2026-08-27

### 新增

- **网页总结携带原文图片**（教程步骤图场景）：
  - 总结抓取正文时同时收集**内容区大图**（优先 article/main 等正文容器；过滤宽 <250px 的图标头像、去重、兼容懒加载 data-src，最多 6 张）；
  - 图片清单随总结请求发给 AI（按出现顺序标注，AI 在要点对应步骤处标注"（图N）"），原图以缩略图挂在总结消息上；
  - **「生成文章」自动带入原图**：润色后的正文按段落间隔均匀插入原文配图（`![原文配图](url)`），编辑器中可删可挪，发布即自带——教程类文章无需再 AI 配图。
- Markdown 渲染支持 `![alt](url)` 图片语法（仅放行 http(s)），聊天气泡与编辑器共用。

## [0.21.4] - 2026-08-27

### 修复

- **AI 助手输入区固定底部**：AI 页外层包裹缺少高度约束，导致整个页面滚动、输入框被卷出视野。现与书签页对齐（内部滚动）：仅消息区滚动，输入框/工具行固定底部；消息到达时自动滚动到底部跟随最新回复。

## [0.21.3] - 2026-08-27

### 变更

- **生成文章面板可见性优化**：生成提示（润色中 / 已润色范围 / 错误）从内容流底部移至顶栏下方**常驻显示**，任何滚动位置都看得到；润色完成进入编辑态时内容区自动滚回顶部（标题与提示优先可见）。

## [0.21.2] - 2026-08-27

### 新增

- **文章标签 AI 生成**：生成文章面板的元信息调用（标题 + SEO）扩展为同时输出 3-5 个核心关键词标签，自动填入标签框（可手动修改，超限自动截断到 5 个 / 每项 20 字）。

## [0.21.1] - 2026-08-27

### 修复

- **润色报「站点返回异常（HTTP 500）」**：服务器日志实证为后端对上游 AI 的 30 秒硬超时——长文润色（8000 字输入）生成耗时超限被掐断，再被 Nginx 错误页拦截显示为纯文本 500。插件端将润色输入收敛到前 3000 字（稳定在超时内），超出部分保留原文并在面板中提示可手动续润。

## [0.21.0] - 2026-08-27

### 新增

- **AI 生成文章全链路**（AI 回答气泡下方「📝 生成文章」按钮）：
  - 润色：调站点 `ai.assist(polish)` 把回答润色到可发布标准，同时由对话模型生成标题与 SEO（可全部手改）；
  - 富文本编辑：插件内建编辑器（加粗/斜体/H2/H3/段落/列表/链接/撤销重做），与主站同为 HTML 格式（`content_format=html`）；
  - **AI 插图**：点击「🖼 插入配图」，按当前文章内容调 `ai.assist(image)` 配图并插入光标处，媒体 ID 自动收集关联（本地缓存同步）；
  - 发布：调开放网关新增的 `posts.create`（凭 Key 绑定用户身份），支持存草稿/直接发布，标签与 SEO 一并提交。

### 后端配套（需重启后端生效）

- 开放网关新增 **发布文章** `POST /api/v1/open/posts`（posts.create，目录已登记）：复用主站 PostService 校验与落库，以 Key 绑定用户为作者；Key 未绑定用户返回 403。

## [0.20.1] - 2026-08-27

### 修复

- **欢迎区「联网搜索」卡片仍显示开发中**：上版能力卡 ready 标记替换未生效（转义匹配问题），已点亮。

### 新增

- **输入框联网开关**：内嵌底栏提示词按钮旁新增地球开关——常态灰色，点击变彩色（强调色底 + 发光文字）视为打开；开启后按发送/回车的消息自动走联网链路（先 SearXNG 检索再作答，回答带来源编号与来源列表），关闭则普通对话。

## [0.20.0] - 2026-08-27

### 新增

- **联网搜索上线**（能力卡点亮，站点自建 SearXNG 聚合 Google/Bing/DuckDuckGo）：
  - 输入框输入问题 → 点「联网搜索」：调 `ai.search` 取标题/摘要/地址（前 5 条）→ 作为参考资料交给站点对话模型生成回答，回答中以［1］［2］标注引用；
  - 回答气泡下方渲染**来源列表**（编号 + 标题，点击新标签打开）；
  - 来源随消息持久化，历史会话翻阅时同样可见；与提示词（角色设定）可叠加使用；
  - 响应解析做了宽松兼容（items/results 数组与常见字段名），后端字段微调无需发版。

## [0.19.0] - 2026-08-27

### 新增

- **生成图片本地缓存**：图片生成成功后除转存站点媒体库外，同时下载 Blob 存入 IndexedDB 本地缓存（manifest 新增 `unlimitedStorage` 权限，容量随磁盘）；对话/历史中的图片渲染优先取本地副本，站点失效或离线仍可查看，未命中自动回退远程地址。发送成文章仍使用站点媒体库通道。
- **AI 历史对话**（工具行 🕘 按钮）：
  - 多会话归档：每次清空（🧹）开启新会话，首条消息自动作为标题；
  - 历史面板：关键词搜索（标题 + 对话内容）、点击翻阅恢复任意会话、单条删除、一键清空全部历史（二次确认）；
  - 会话按更新时间倒序，显示条数与相对时间。

## [0.18.0] - 2026-08-27

### 变更

- **点击悬浮球 = 召唤侧边栏（三级打开）**：
  - ① Chrome 原生侧边栏（经 background `sidePanel.open`，消息链由用户手势引发）；
  - ② 无 sidePanel API 的浏览器（如 Edge）→ 球所在页面的页内停靠侧栏（新增 `yy-dock-open` 幂等打开）；
  - ③ 全都不可用（如 SW 未响应）→ 展开球面板兜底。
- **点击必达加固**：pointerup 与 click 双通道响应单击（先到者生效、另一条被抑制），拖动结束继续抑制补发 click——修「点击没反应」。

## [0.17.3] - 2026-08-27

### 修复

- **点击悬浮球打开/关闭面板**：面板开关此前挂在 `pointerup` 上，受指针捕获与拖拽判定的边缘情况影响偶发失效。改用浏览器保证完整点击语义的 `click` 事件（拖动结束补发的 click 自动抑制一次，不误触切换）。
- **吸附状态污染**：`peekOut` 滑出时的临时坐标会触发吸附态自动推断被冲掉（吸附缩回失效）。吸附态改为仅在拖动吸附与启动恢复两处显式维护。

## [0.17.2] - 2026-08-27

### 修复

- **菜单「一会消失一会打开」的闪烁循环**：事件语义错用——`pointerenter` 不冒泡且指针从 DOM 后代移到祖先时不会触发祖先的 enter，导致「进入感应区取消收起」的取消信号永远收不到：定时收起到期关菜单 → 鼠标落回球上又重新打开 → 循环闪烁。取消信号改用冒泡的 `pointerover`（子树内任何命中变化都会经过感应区），离开整个子树才由 `pointerleave` 延时收起。悬停链路：球 ↔ 菜单全程稳定，吸附滑出场景同样覆盖。

## [0.17.1] - 2026-08-27

### 修复

- **菜单移动途中消失（0.16.3 的间隙回归）**：感应区为保拖拽改为永久穿透后，球与按钮之间的空隙无命中目标，鼠标一离开球即触发收起。现感应区移至球体下方（与球底 4px 微重叠衔接），菜单展开时恢复实体命中，并以「离场延时 140ms 收起 + 任一区域进入即取消」衔接球↔菜单的移动路径。
- 悬停收起时若处于边缘吸附态，一并缩回半隐藏。

### 变更

- **球心图案改为月亮**：去掉「月」字图标，改为 CSS mask 挖出的月牙图案，配双层 drop-shadow 光斑（月牙形状本身发光）。

## [0.17.0] - 2026-08-27

### 新增

- **边缘吸附半隐藏**：拖动悬浮球靠近左/右屏幕边缘松手即自动吸附，球体一半藏进屏幕外；鼠标移上去平滑滑出显示全部，移开后又缩回半隐藏。吸附位置照常记忆，重开页面保持。
- **月亮视觉特效**：
  - 球体改为月面质感（月白径向渐变 + 左上柔光斑）；
  - 月晕呼吸层：银蓝色光晕以 3.4 秒周期微微地明暗胀缩；
  - 四颗黄色小星星散布月面，各自以不同节奏/相位若隐若现地闪烁（透明度与缩放旋转渐变）。
- 拖动时关闭位移过渡保证跟手，松手吸附/滑出时恢复过渡（平滑动画）。

## [0.16.3] - 2026-08-27

### 修复

- **悬浮球无法拖动（0.16.2 回归）**：感应区展开时 `pointer-events:auto` 且覆盖球体，球被自己的感应区挡住、pointerdown 落空。悬停模型重构：球与菜单按钮统一挂入感应区子树，感应区自身**永久穿透**（不接收指针），仅球体（显式 `pointer-events:auto`）与菜单按钮（展开态 auto）接收指针——拖拽、悬停展开、菜单选择三者互不遮挡。

## [0.16.2] - 2026-08-27

### 修复

- **悬停菜单「一移过去就消失、无法选中」**：收起监听挂在感应区的 `pointerleave` 上，而菜单按钮是感应区的兄弟节点——鼠标从球移向按钮的瞬间命中目标切换，浏览器判定离开感应区立即收起。已把菜单按钮移入感应区 DOM 子树（指针停留在后代上不算离开），移动路径全程稳定可选中。

## [0.16.1] - 2026-08-27

### 变更

- **悬浮球菜单改为纵向排列**：悬停后三个按钮在球正下方依次向下展开（等距、水平居中对齐球心），鼠标直线下移即可选择，比扇形更好命中。

### 修复

- **悬浮球开关「要刷新页面才生效」**：根因是扩展刷新/更新后，已开页面里的旧脚本上下文失效（`chrome.runtime` 断开，storage 监听与菜单点击全部失灵），刷新页面才能换上新脚本——开发迭代期频繁触发，正式更新扩展后同样会发生。本轮加固：
  - 僵尸自愈：切回标签页或悬停球时检测上下文，失效即自动移除旧球（不再挂着死球）；
  - 交互兜底：每次悬停球时同步一次开关状态（不单依赖 storage 监听）。

## [0.16.0] - 2026-08-27

### 新增

- **悬浮球扇形菜单**：鼠标移上悬浮球，上方以扇形展开三个快捷入口（带回弹动画，移出感应区自动收起）：
  - ⭐ **一键收藏**：当前页直接存入插件书签夹（按 URL 归一化去重，重复收藏提示「已在书签中」，完成后球旁气泡反馈）；
  - 📝 **网页总结**：展开球面板并自动切到 AI 页，对当前网页执行总结；
  - 📸 **网页截图**：展开球面板并自动进入区域截图分析流程（页面上拖框选区识别）。
- 执行链路：菜单动作写入 `panel_action` 暂存 + runtime 广播，由球展开的 embed 面板消费执行（挂载检查与消息双通道防丢；仅 embed 面板响应，避免侧栏/浮窗实例重复执行）。

## [0.15.1] - 2026-08-27

### 修复

- **识图/生图报"无法连接站点"（超时误报）**：请求层此前写死 15 秒超时，`AbortController` 触发的中断与真实网络故障都落进同一个 catch，文案误导为"无法连接站点"。多模态识图与文生图上游耗时经常超过 15 秒。现：
  - 超时按任务分级：普通 GET 15s / AI 对话 60s / 识图与文生图 90s；
  - 超时与断网的报错文案分离（超时明确提示"站点处理时间过长，请稍后重试"）。

## [0.15.0] - 2026-08-27

### 变更

- **截图分析改为区域截图**：点击能力卡后在当前网页上注入一次性取景器——半透明遮罩 + 拖拽画虚线框（Esc 取消、过小框视为误触取消），松手后仅裁剪选中区域识别：
  - 选区以 CSS 像素记录，裁剪按 `devicePixelRatio` 换算物理像素，高清屏不糊；
  - 裁剪后继续走本地压缩（目标 <800KB），区域小图识别更快更准；
  - 消息文案改为「✂️ 区域截图分析」。

## [0.14.1] - 2026-08-27

### 修复

- **截图分析报"无法连接站点"**：全屏截图 base64 常超 1MB，站点前置反向代理（Nginx 默认 `client_max_body_size 1m`）直接拒绝且错误响应无 CORS 头，浏览器表现为 fetch 网络层失败。现截图后在插件内用 canvas 等比缩放 + JPEG 分档压缩（长边 1280 起、逐档降级，目标 <800KB）再上传，识图精度不受影响。
  - 若仍被拒（如代理限制低于 800KB），建议服务端调大 `client_max_body_size`（如 5m）。

## [0.14.0] - 2026-08-27

### 新增

- **图片生成**（能力卡点亮）：输入画面描述 → 站点 `ai.assist(image)` 文生图（MiniMax image-01），生成图已由后端转存站点媒体库（永久有效），对话内直接渲染展示、点击放大新标签查看。
- **截图分析**（能力卡点亮）：`chrome.tabs.captureVisibleTab` 截取当前可见页面 → 以 data URL 直传站点 `ai.assist(recognize)` 多模态识图（后端 `resolveImageInput` 原样透传 data URL，无需公网地址）→ 识别描述以 Markdown 渲染返回；截图缩略图随消息展示。
- 消息结构支持附带图片（`images`）；持久化时自动剥离截图 dataUrl（体积考虑，重开后缩略图消失、文本保留）。
- 按需主机授权逻辑抽公用（网页总结与截图分析共用）。

## [0.13.1] - 2026-08-27

### 变更

- **发送区排版优化**：输入框改为内嵌底栏——「选择提示词」按钮与圆形发送按钮收纳进输入框内部（下缘一行，左右分布），聚焦时整框高亮；原独立状态行移除。
- **在线状态移至顶栏**：用户昵称右侧显示实时联网小圆点与「在线/未联网」文案（所有 Tab 可见，不再仅 AI 页）。

## [0.13.0] - 2026-08-27

### 变更

- **用户气泡不再展示整页正文**：网页总结/文件解析的用户消息只显示摘要（如「🌐 标题 — 网址」「📄 文件名」），完整正文仅在后台作为上下文发给 AI（多轮追问时仍携带，AI 记忆不受影响）。
- **AI 回复渲染 Markdown**：标题、加粗、斜体、行内代码、代码块（横向滚动）、有序/无序列表、引用、链接、分隔线；渲染前整体 HTML 转义防 XSS，链接仅放行 http(s)。

## [0.12.0] - 2026-08-27

### 修复

- **网页总结彻底改道 `chrome.scripting` 动态注入**：不再依赖页面预先注入的内容脚本（该方案对"扩展更新后脚本失效、需刷新页面"敏感，是上版仍失败的原因）。现点击瞬间向目标标签注入一次性抓取函数，**页面无需刷新**、扩展更新后立即可用——与主流竞品实现一致。
  - manifest 新增 `scripting`、`activeTab` 权限与 `optional_host_permissions`（http/https）；
  - 首次点击「网页总结」会弹出「读取网站信息」授权框，同意一次长期可用；拒绝时给出开启路径提示；
  - 抓取函数在页面隔离环境执行、自包含，仅返回标题/地址/正文文本。

## [0.11.0] - 2026-08-27

### 修复

- **「未找到可总结的网页」误报（真正根因）**：Chrome 官方规则——manifest 未声明 `"tabs"` 权限时 `chrome.tabs.query` 返回的 `tab.url` 恒为 `undefined`，导致候选标签全部被判为"不可注入"。已补 `tabs` 权限（竞品同类插件的通行做法），并把判定逻辑改为「消息能否送达并返回正文」为准：
  - 聚焦窗口活动标签 → 其余窗口活动标签逐个尝试；
  - 失败按原因分类提示：浏览器内部页（切到普通网页）/ 扩展刚更新过（刷新该网页即可恢复内容脚本）/ 无网页标签。
  - v0.8 起历次"不支持网页总结"的误报均源于此权限缺失，本轮彻底修复。

## [0.10.1] - 2026-08-27

### 变更

- **网页总结改为一步直达**：移除「输入 URL 抓取」流程（弹窗、正文提取爬虫、按站点动态授权一并撤销）——点击能力卡「网页总结」或工具行 🌍 按钮，即直接读取当前打开的网页正文并交给 AI 总结（跨窗口自动定位用户正在看的活动标签）；manifest 同步移除 `optional_host_permissions`，恢复零主机权限。

## [0.10.0] - 2026-08-27

### 新增

- **提示词功能**（AI 助手输入区左下角「提示词」按钮）：
  - 向上弹出「选择提示词」面板，预置 10 条角色提示词：软件推荐管家、万能绘图、提示词生成专家、语义错别字检察员、自然语言转Emoji、小红书爆款文案、中英翻译专家（信达雅）、代码解释器、朋友圈/小红书文案助手、法律/合同条款解读专家；
  - 「＋ 新增」支持自定义提示词（名称 + 内容，存本地可随时删除）；
  - 选中后输入框上方出现徽标（可一键停用），后续对话以该角色设定发送。

### 变更

- 发送区布局对照设计稿调整：输入框下方一行——左侧「提示词」按钮与联网状态、右侧圆形发送钮。

## [0.9.0] - 2026-08-27

### 修复

- **「网页总结」误报"仅支持普通网页"**：悬浮窗形态聚焦时 `lastFocusedWindow` 命中的是无标签页的浮窗，导致查不到目标页。现改为两级候选：聚焦窗口活动标签 → 其余普通窗口的活动标签，仅命中可注入页面。

### 新增

- **抓取任意网页（URL 爬虫）**：工具行新增 🌍 按钮，输入网址即可抓取正文交给 AI 总结：
  - 首次抓取某站点时按 `optional_host_permissions` 动态申请该站点访问授权（用户可拒绝，仅授权使用过的站点，保持最小权限）；
  - 纯前端正文提取（DOMParser 剥离脚本/样式/导航噪音，优先命中 article/main 等主内容容器），原文不出本机，仅正文交站点 AI。
- 内容脚本支持 `file://` 本地页面（需在扩展详情开启「允许访问文件网址」），本地 HTML 文档也可总结。

## [0.8.0] - 2026-08-27

### 新增

- **AI 助手页对照设计稿重做**：
  - 欢迎区（👋 你好 / 我是月言 AI 小助手）+ 六宫格能力卡片；
  - 能力实现：**智能问答**（站点 AI 对话）、**文件解析**（本地读取文本类文件交 AI 解析，≤1MB，无隐私上传）、**网页总结**（抓取当前标签页正文交 AI 总结，内容脚本仅响应用户主动点击）；联网搜索 / 图片生成 / 截图分析因后端暂无对应开放能力，标注「即将上线」不提供假功能；
  - **上下文来源条**：文件/网页作为参考资料后出现，附「提问」「总结」快捷动作；
  - 模型胶囊下拉选择、📎 附件、🧹 清空对话工具行；
  - 输入框（Enter 发送 / Shift+Enter 换行）+ 底部状态行（在线/未联网实时指示 + 发送钮）；
  - 对话记录本地持久化（`ai_chat_v1`），重开面板不丢，可随时清空。

## [0.7.2] - 2026-08-27

### 修复

- **旧版导入丢失深层分类**：v0.6.x 的导入把多层文件夹压平成一层（如「AI / 子分类」变成单个名字），且迁移后重复导入会因 URL 已存在而跳过，导致「AI 文件夹下还有子分类」无法呈现。现于管理菜单新增**「♻️ 重置为完整层级」**：二次确认后清空当前书签夹，按浏览器书签的真实嵌套结构全深度重建。

## [0.7.1] - 2026-08-27

### 修复

- **文件夹折叠状态持久化**：收起/展开状态此前仅存于内存，重开面板即重置。现写入 `chrome.storage.local`（`bookmarks_collapsed_v1`），下次打开面板恢复上次状态；「未分类」区块同样生效。

## [0.7.0] - 2026-08-27

### 新增

- **多级文件夹**：书签存储升级为树形结构（`bookmarks_v2`），支持文件夹嵌套子文件夹；浏览器书签导入不再限制两层，全深度嵌套映射。旧扁平结构首次读取时自动迁移，不丢数据。
- **「未分类」可折叠**：根级散链区块与普通文件夹行为一致，点击头部收起/展开。
- **编辑模式拖拽排序**：进入编辑后按住任意书签或文件夹行即可鼠标拖动，松手落在目标行的上/下半区插入其前/后（可跨层级移动、文件夹整体搬运）；自动阻止把节点拖进它自己的子树（防环）。
- 拖拽时被拖条目半透明提示；书签页顶部给出操作指引文案。

## [0.6.1] - 2026-08-27

### 新增

- **文件夹折叠**：点击分组头即可收起/展开该文件夹下的书签（未分类组固定展开），折叠状态在本次会话内记忆。
- **站点小图标**：书签条目改为「站点图标 + 标题」单行样式（完整地址移入 hover 提示）；图标经 Chrome 内置 `_favicon` 协议读取浏览器本地图标缓存（零网络请求），无图标的站点自动回退默认图标。

### 权限

- manifest 新增 `favicon`（使用内置 `_favicon` 图标协议所需）。

## [0.6.0] - 2026-08-27

### 新增

- **书签页**（取代「接口中心」，接口中心后续版本再回归）：本地书签夹完整管理，不依赖站点连接：
  - 列表分组展示（文件夹/未分类），点击新标签打开；关键词实时搜索；
  - 底部工具栏：搜索框 + 「＋ 新建/导入」上弹菜单（导入书签 / 新建文件夹 / 新建书签）+「🛠 管理」上弹菜单（**书签有效性检测**：逐条探测并标注可达红绿点与汇总 / **书签查重**：URL 归一化识别重复组并一键清理保留最早）+「✏️ 编辑」（横向操作条：全选、删除——含二次确认、移动到文件夹、完成）；
  - 空状态引导：「导入本地书签」（读取浏览器书签树，按文件夹映射两层深度的路径命名并去重导入）/「新建书签」。

### 变更

- manifest 权限新增 `bookmarks`（供「导入本地书签」读取浏览器书签，符合权限三问）。
- 底部 AI 快捷输入框调整为仅首页展示，为书签页让出完整空间。
- 首页功能卡「接口中心」替换为「我的书签」。

### 修复

- **原生侧边栏从未生效**：manifest 漏声明 `sidePanel` 权限，导致 `chrome.sidePanel` 对象不可用、能力探测恒为 false——即使浏览器完全支持（如 Chrome 114+），也始终降级为页内停靠。补上权限后，支持的浏览器点击图标即恢复真正的原生侧边栏；不支持的浏览器仍自动走页内停靠降级。
- 同步修正手册 §5.1 manifest 模板与 §6 权限规范说明。

## [0.5.0] - 2026-08-27

### 新增

- **页内停靠侧栏**（修复「无侧边栏按钮」）：无 `chrome.sidePanel` API 的浏览器（如 Edge）此前不显示「浏览器右侧」按钮、无法停靠——现新增 content script 在网页右缘注入全高侧栏（430px，窄屏自适应），与原生侧边栏体验等效。
- 打开方式升级为**三级降级**：点击图标与顶栏「浏览器右侧」按钮按 ① 原生侧边栏 → ② 页内停靠 → ③ 独立悬浮窗的顺序自动选择；`chrome://` 等不可注入页面自动落到悬浮窗。

## [0.4.2] - 2026-08-27

### 修复

- **悬浮球点击无反应**：closed Shadow Root 下宿主页事件的 `composedPath()` 会被浏览器剪枝，「点外面收起」监听器因此误判球体自身的点击也在面板外，同一手势把刚打开的面板立即关闭。改为以 retarget 后稳定可见的宿主容器 `host` 判定点击来源。

## [0.4.1] - 2026-08-27

### 修复

- **HTTP 站点请求被浏览器拦截的问题定位与提示**：明文 HTTP 站点在安全环境（尤其 HTTPS 网页内的球形悬浮面板）会被「混合内容策略」硬性拦截，属平台安全规则，插件无法绕过。本次：
  - 网络失败的报错文案改为明确说明拦截原因与解决路径；
  - 连接表单检测到非本机的 `http://` 地址时显示黄色预警。

### 使用建议

为公网部署的 boke 配置 HTTPS（Nginx/Caddy 反向代理 + 域名证书），否则球形悬浮在任意 HTTPS 页面都无法工作；`http://localhost` 本地开发不受影响。

## [0.4.0] - 2026-08-27

### 新增

- **顶栏开关组**：右上角新增三个一键开关（当前生效项高亮实底显示）：
  - 「网页悬浮球」：点亮即在全网页显示球形入口，熄灭即隐藏（与「站点连接」弹层内的开关双向同步）；
  - 「面板悬浮」：以独立悬浮窗打开面板；
  - 「浏览器右侧」：停靠为原生侧边栏。
- service worker 在 manifest 声明 `type: "module"`，规避经典脚本解析限制的缓存复发。

## [0.3.0] - 2026-08-27

### 新增

- **球形悬浮**：在浏览的任意 http(s) 网页上注入可拖动的圆形浮球入口（右缘中部默认停靠），点击展开页面内嵌完整面板（392×600 iframe），点球或面板外区域收起；位置自动记忆。
- 「站点连接」管理弹层新增「球形悬浮」开关，关闭后所有网页的浮球即时消失（storage 实时监听）。
- 安全设计：浮球与面板均渲染于 closed Shadow DOM / 扩展 iframe 中，不读取宿主页面任何内容。

## [0.2.0] - 2026-08-27

### 新增

- **面板形态切换**：顶栏新增切换按钮，可在「浏览器右侧栏」与「悬浮窗」（420×780 独立小窗）之间一键互切：
  - 侧栏 → 悬浮：新开浮窗并自动收起右侧栏（Chromium 无 sidePanel.close，采用 setOptions 开关技巧收起，失败时保留面板不影响使用）；
  - 悬浮 → 侧栏：自动定位宿主主窗口展开侧栏并关闭浮窗。
- 点击工具栏图标默认在右侧原生侧边栏打开；Edge 无 sidePanel API 的环境下隐藏切换按钮（只有浮窗形态），其余行为不变。

## [0.1.0] - 2026-08-26

### 新增

- 侧边栏形态：点击工具栏图标打开插件面板；Chrome（114+）使用原生侧边栏，Edge / 无 sidePanel API 环境自动降级为同尺寸独立小窗，共用同一份页面。
- 站点连接（登录）：未连接时整屏欢迎页引导；填写「站点 URL + 开放接口 Key」完成连接，自动拉取站点信息与绑定用户的公开资料（昵称、头像、计数）。
- 连接管理弹层：点击顶栏头像或设置齿轮打开，支持查看当前身份与 Key 掩码、更换配置重连、断开连接。
- 首页 Tab：问候区 + 功能导航卡宫格 + 最新帖子预览（开放接口 posts.list）。
- AI 助手 Tab：模型选择（ai.models）+ 多轮对话（ai.chat）；首页底部快捷输入框可直接跳转发送。
- 接口中心 Tab：授权语义说明 + 当前站点信息 + 13 条开放接口目录清单。
- 双主题：复用主站 tokens.css 设计令牌（冷月暗色 / 薄雾亮色），顶栏一键切换并持久化。

### 后端配套

- 迁移 021：`open_api_keys` 增加 `user_id`，生成 Key 自动绑定当前管理员。
- 新增开放接口 `me.profile`（GET `/api/v1/open/me`）：凭 Key 返回绑定用户公开资料；旧存量 Key 未绑定时返回明确提示。
- CORS：Allow-Headers 追加 `X-Api-Key`；`/api/v1/open/` 路径回显任意 Origin（浏览器扩展跨域直连前提）。

### 使用前提

- 在主站后台「接口开放」重新生成一个 Key（勾选需要的接口，至少含「我的资料」），旧的未绑定用户的 Key 无法获取身份信息。
